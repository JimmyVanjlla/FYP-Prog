fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt self-test breaks on bcrypt>=4.1 (AttributeError on
# bcrypt.__about__, then a bogus "password too long" from its internal wrap-
# bug probe) — pin to a version known to work with it.
bcrypt==4.0.1
python-multipart==0.0.19
pytest==8.3.4
httpx==0.28.1

# Sprint 2: Demand Forecasting + background processing tier (Ch4 §4.1)
celery==5.4.0
redis==5.2.1
pandas==2.2.3
prophet==1.1.6

